Aim: "Directly chat encrypted with someone"


- init_ceofhack.sh:
   - create configuration directory
   - create pgp configuration:
      - create gpg-keys without password  below ~/.ceof/pgp (the pgp home of ceof)
      - documentate that in doc/EOF.tex
   - copy and enable transport protocol tcp (via netcat)
      - listen on tcp:0.0.0.0:6667
   - print hint on how to start ceofhack and add peers

- create commands in ceofhack (use existing structure):
   - check current doc, whether the following is right:
      - /peer add <name> <address> (make it possible to add a peer with an initial address)
      - /key add <name> <fingerprint> (make it possible to add a key fingerprint)
      - /msg <name> <message> (make it possible to send messages)

- transport protocol:
   - create & check: listen & send for tcp
   - cleanly documentate in EOF.tex
      - commands for transport protocols
      - how to use stdin/stdout/stderr

- check all structures and functions for programming errors
   - valgrind
   - electric fence

- add minimal usage doc in EOF.tex:
   - cleanup the imported documents, check if everything is up-to-date (basic.tex)
   - TP: tcp: syntax, available implementations
   - how to create keys (with script and manually)
   - usable to promote EOF-2
      - doc matches code
      - add implemented and planned

- general: implement all commands tagged
   "Implement in ceofhack-0.1!" in doc/EOF.tex

--------------------------------------------------------------------------------

Could also fit into 0.2, but would be nice for 0.1:

- create macros:
   - maybe reuse report.c from gpm (license is ok)

   - error(char *str, ...); 
   - warn(char *str, ...); 
   - info(char *str, ...); 
   - debug(char *str, ...);
   
   - which use the function report(int level, char *str, ...)
   - using __FILE__ and __LINE__ in output
   - error/warn print to stderr
   - info to stdout
   - debug only if debug=1 (command line switch or compile option)
   - currently no other differences, but framework for later
      - may use different prefixes

- replace all printf()s with macros
